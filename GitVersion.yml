mode: ContinuousDeployment
next-version: 2.0.0

branches:
  main:
    increment: Minor
    prevent-increment-of-merged-branch-version: true
    tag: rc

  release:
    regex: ^release[/-]
    increment: Patch
    tag: ""

ignore:
  sha: []
