mode: ContinuousDeployment
next-version: 1.0.0

branches:
  main:
    increment: Minor
    prevent-increment-of-merged-branch-version: true
    tag: alpha

  release:
    regex: ^release[/-]
    increment: Patch
    tag: ""

ignore:
  sha: []
